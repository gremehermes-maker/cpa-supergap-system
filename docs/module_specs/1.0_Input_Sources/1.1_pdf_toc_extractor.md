# 1.1 기본서 PDF 목차 추출 및 강제 주입 시스템 (Hybrid Workflow)

## 1. 개요 및 설계 철학
이 모듈은 과거의 복잡한 정규식 폰트 파싱을 폐기하고, **"AI 텍스트 분석 기반의 초벌 추출 + 인간의 시각적 튜닝 + PDF 원본 자체에 북마크(목차)를 강제 주입(Injection)"**하는 3단계 하이브리드 워크플로우로 최종 진화했습니다. 

마진노트4(MarginNote 4)가 외부 마인드맵(OPML 등) 가져오기를 차단함에 따라, 처음 PDF를 불러올 때 사용자가 수백 개의 단원을 일일이 타이핑하는 노가다를 없애고 단 한 번의 클릭(목차 가져오기)으로 **마인드맵 뼈대를 자동 구축**하기 위해 PDF 파일 자체의 심장부에 우리가 원하는 완벽한 목차를 강제로 꽂아 넣는 초격차 우회 솔루션을 도입했습니다.

## 2. 3단계 하이브리드 파이프라인 작동 원리

### Phase 1: AI 목차 초벌 추출 (Automated Text Extraction)
파이썬 스크립트가 기본서 PDF의 텍스트를 자동으로 추출하여 AI에게 제공합니다.
AI는 추출된 텍스트와 프롬프트 규칙을 기반으로 오차 없이 구조화된 JSON 데이터(`_toc.json`) 초안을 즉시 뽑아냅니다.

### Phase 2: Python GUI 시각적 튜닝 (`1.1_pdf_toc_extractor.py`)
AI가 만들어준 `_toc.json` 초안을 불러와서, 윈도우 폴더 형식(Treeview)으로 시각화하는 파이썬 GUI 프로그램입니다.
* **핵심 조작:** 마우스 드래그 앤 드롭(Drag & Drop)을 통한 직관적인 노드 상하/순서 계층 변경
* **노드 관리:** 추가, 수정, 삭제 기능 지원

### Phase 3: PDF 물리적 북마크 주입 (Injection via PyMuPDF)
사용자가 GUI 튜닝을 마치고 **[최종 저장 및 PDF 북마크 주입]** 버튼을 누르면 다음 작업이 일어납니다.
1. 향후 1.4 모듈(DB 연동)을 위한 `_toc.json` 저장
2. 타겟 원본 PDF 파일을 열고, 파이썬 `PyMuPDF (fitz)` 라이브러리를 통해 방금 튜닝한 완벽한 트리 구조를 PDF의 '공식 북마크(Outline)'로 강제 덮어쓰기
3. `원본이름_목차주입본.pdf` 파일로 새로 생성됨

## 3. 입출력 스펙 및 핵심 워크플로우 규칙

- **입력:** 기본서 원본 PDF 파일, AI가 추출한 `_toc.json` 초안
- **출력:** 최종 검증된 `_toc.json` (DB용) 및 **북마크가 주입된 `_목차주입본.pdf`**

> [!WARNING]
> **마진노트 4 연동 시 절대 규칙: 투 트랙(Dual-Track) 전략**
> 마진노트 4는 문서 내부 병합 기능이 제한적이며, 외부에서 파일을 합쳐서 가져오면 파일 해시(MD5)가 변경되어 기존 마인드맵 링크가 전부 파괴됩니다. 이를 방지하기 위해 다음 원칙을 반드시 지켜야 합니다.
> 
> **[마진노트 4 ↔ 굿노트 연동 투 트랙 전략]**
> 1. **마진노트 4:** 1.1 모듈을 통해 **목차가 주입된 깨끗한 원본 분할 PDF**를 딱 한 번 스터디 셋에 추가하고, 평생 파일을 덮어쓰거나 교체하지 않습니다. (마인드맵 뼈대 구축 및 발췌 전용 창고)
> 2. **굿노트:** 필기 업데이트는 굿노트에서 별도로 자유롭게 진행합니다.
> 3. **통합:** 굿노트에서 필기한 특정 핵심 페이지만 캡처하거나 한 장짜리 PDF로 빼내서, 마진노트 스터디 셋에 **'별도의 첨부 문서(자식 문서)'**로 던져 넣습니다. 
> 
> 이 원본 보존 방식만이 마진노트 4의 해시 구조를 거스르지 않고 마인드맵 파편화를 0%로 만드는 유일한 생존 전략입니다.
